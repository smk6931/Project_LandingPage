[2026-01-21]

1. 데이터베이스(PostgreSQL) 구축 완료
   - Docker Compose를 활용하여 DB 컨테이너 세팅 (landing-db)
   - 포트 충돌 해결: 기존 5432 -> 신규 5433 포트 할당
   - 서버 내 Docker 실행 및 정상 작동 확인

2. DB 외부 접속 환경 구성 (보안 강화)
   - AWS 스타일의 SSH Tunneling 보안 접속 방식 채택
   - DB 포트(5433)를 외부(0.0.0.0)에서 닫고, 내부(127.0.0.1)에서만 열도록 docker-compose.yml 수정
   - pgAdmin을 통한 SSH 터널링 접속 테스트 완료

3. 문제 해결 (트러블슈팅)
   - 이슈: docker-compose.yml에서 비밀번호를 "1234"로 변경했으나 적용되지 않는 현상
   - 원인: 바인드 마운트된 데이터 폴더(postgres_data)가 `down -v`로 삭제되지 않아 예전 데이터 유지됨
   - 해결: `rm -rf postgres_data`로 폴더 직접 삭제 후 재기동하여 정상 적용 확인

4. 향후 계획
   - 로컬 React & FastAPI와 서버 DB 연동 테스트
   - 견적 문의 폼 데이터 저장 로직 구현
